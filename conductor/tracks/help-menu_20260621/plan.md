# Implementation Plan: Opção "Help" no Menu e Separadores de Grupo

**Track ID:** help-menu_20260621
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-21
**Status:** [ ] Not Started

## Overview

Feature majoritariamente de UI em vanilla TypeScript. A abordagem reaproveita os padrões
existentes: estrutura de menu em `index.html`, lógica em `src/main.ts`, estilos em
`src/style.css` e textos em `src/strings.ts`. O modal de Help espelha o padrão do modal
"About" (overlay + panel, fechar por botão/overlay/Escape). Conforme o workflow do projeto,
não há testes obrigatórios para UI; a verificação é manual, apenas no fechamento do track.

## Phase 1: Strings (i18n em inglês)

Adicionar todas as chaves de texto necessárias na fonte única `src/strings.ts`, seguindo o
agrupamento por contexto e o padrão `data-i18n` existente.

### Tasks

- [x] Task 1.1: Adicionar chave `menu.help` ("Help") na seção do menu de comandos.
- [x] Task 1.2: Adicionar chaves do modal de Help (título, intro e bullets de
      funcionalidades + texto do botão de fechar e `aria-label` de acessibilidade).
- [x] Task 1.3: Garantir tipagem (`StringKey`) e consistência com as chaves existentes.

### Verification

- [x] `npm run build`/typecheck passa sem erros de tipo nas novas chaves.

## Phase 2: Estrutura do Menu (item Help + separadores)

Inserir o item "Help" como primeiro item do `#commands-menu` e adicionar os separadores de
grupo, com o CSS correspondente.

### Tasks

- [x] Task 2.1: Em `index.html`, adicionar `<button id="cmd-help" class="menu-item"
      role="menuitem" data-i18n="menu.help">` como primeiro item (antes de Import).
- [x] Task 2.2: Em `index.html`, adicionar `<div class="menu-divider">` entre os grupos:
      após Help e após Clear DB (deixando About isolado no último grupo).
- [x] Task 2.3: Em `src/style.css`, adicionar a classe `.menu-divider` (linha fina).

### Verification

- [x] O menu exibe Help no topo e 3 grupos visualmente separados por linha fina.

## Phase 3: Modal de Help (markup + lógica)

Criar o overlay/panel do Help espelhando o modal "About" e conectar a lógica de
abrir/fechar em `src/main.ts`.

### Tasks

- [x] Task 3.1: Em `index.html`, adicionar `#help-overlay` + `#help-panel` (`role="dialog"`,
      `aria-modal`, `aria-labelledby`), com título, conteúdo (placeholder de bullets) e
      botão `#help-close`, espelhando a estrutura do `#about-panel`.
- [x] Task 3.2: Em `src/main.ts`, capturar os elementos do Help e implementar
      `openHelp()`/`closeHelp()` (definindo `hidden`).
- [x] Task 3.3: Conectar os eventos: clique em `#cmd-help` abre o Help e fecha o menu;
      botão de fechar, clique no overlay e tecla Escape fecham o Help.
- [x] Task 3.4: Garantir que Help e About não abram simultaneamente / não conflitem.

### Verification

- [x] Abrir/fechar o Help funciona pelos três meios (botão, overlay, Escape) sem afetar o
      About nem o menu.

## Phase 4: Conteúdo do Help + polimento

Preencher o conteúdo do modal com o resumo das principais funcionalidades e ajustar
estilos/acessibilidade.

### Tasks

- [x] Task 4.1: Rascunhar o conteúdo do Help (bullets concisos: paleta/arrastar componentes,
      conectar pinos, alternar entradas e clock, criar/editar chips com "Make", simulação em
      tempo real, persistência local, tutorial interativo) e ligar via `data-i18n`.
- [x] Task 4.2: Ajustar CSS do `#help-panel`/conteúdo (reuso de estilos do About quando
      possível; espaçamento dos bullets) e revisar responsividade em tela pequena.
- [x] Task 4.3: Revisar acessibilidade (foco, labels) e consistência visual com o About.

### Verification

- [x] Conteúdo legível e bem formatado; visual consistente com o restante da UI.

## Final Verification

- [ ] Todos os critérios de aceitação do spec atendidos.
- [ ] `npm run dev` rodando; roteiro de teste manual executado pelo usuário.
- [ ] Conteúdo do Help revisado e aprovado pelo usuário.
- [ ] Pronto para revisão/commit (Conventional Commits).

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
