# Implementation Plan: Tradução da Interface para Inglês (com base p/ i18n)

**Track ID:** translate-ui_20260604
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-04
**Status:** [ ] Not Started

## Overview

Criar um módulo central de strings em inglês (`src/strings.ts`) com chaves tipadas e
suporte a placeholders. Wirar os textos estáticos do `index.html` para serem preenchidos
em runtime (atributos `data-i18n` + bootstrap que varre o DOM). Substituir as strings de
UI dinâmicas em `main.ts` e `chip.ts` por referências ao módulo. Validar manualmente no
fechamento (checkpoint único, conforme workflow).

## Phase 1: Módulo central de strings

Criar a fundação: um único módulo com todas as strings de UI em inglês, chaves agrupadas
por contexto e tipadas, com helper para interpolação de placeholders.

### Tasks

- [x] Task 1.1: Criar `src/strings.ts` com objeto `STRINGS` agrupado por contexto
      (ex.: `palette`, `editBar`, `nameDialog`, `actions`, `confirm`, `validation`,
      `a11y`, `meta`), tipado (`as const`), em inglês.
- [x] Task 1.2: Adicionar helper de interpolação `t(template, params)` para textos com
      placeholders (ex.: `Editing: {name}`, `A chip named "{name}" already exists.`).
- [x] Task 1.3: (se aplicável) Cobrir o helper de interpolação com teste unitário
      (`src/strings.test.ts`) — lógica simples mas verificável.

### Verification

- [x] `STRINGS` exporta todas as chaves do inventário da spec; `tsc` sem erros; teste do
      helper passando (se criado).

## Phase 2: HTML estático via módulo (data-i18n + bootstrap)

Remover textos hardcoded do `index.html` e preenchê-los a partir do módulo central no
carregamento da página.

### Tasks

- [x] Task 2.1: Anotar elementos do `index.html` com `data-i18n` (texto) e
      `data-i18n-attr` (para `aria-label`/`placeholder`/meta), removendo os textos PT
      hardcoded; manter "NAND" e o `<title>` (nome próprio). Trocar `lang="pt-BR"` →
      `lang="en"`.
- [x] Task 2.2: Criar bootstrap `applyStrings(root)` (em `src/strings.ts` ou
      `src/i18n.ts`) que varre `[data-i18n]` / `[data-i18n-attr]` e injeta os textos do
      módulo; chamar no início do `main.ts`.
- [x] Task 2.3: Garantir ordem de execução — strings aplicadas antes de qualquer leitura
      de texto da UI dependente.

### Verification

- [x] App carrega com todos os textos estáticos em inglês; nenhum texto PT hardcoded
      remanescente no `index.html` (exceto `<title>`/NAND).

## Phase 3: Strings dinâmicas em TS

Substituir as strings de UI geradas dinamicamente por referências ao módulo central.

### Tasks

- [x] Task 3.1: `main.ts` — `nameConfirm.textContent` (Save/Create), `editLabel`
      (`Editing: {name}` via `t()`), e o `confirm()` do Clear DB passam a usar `STRINGS`.
- [x] Task 3.2: `chip.ts` — mensagens de validação de nome (`reason`) passam a usar
      `STRINGS`/`t()` em inglês.
- [x] Task 3.3: Buscar por strings de UI em PT remanescentes no `src/` (excluindo
      dev-facing acordados) e migrar quaisquer pendências.

### Verification

- [x] Nenhuma string de UI em PT em `main.ts`/`chip.ts`; mensagens de validação e labels
      dinâmicos aparecem em inglês.

## Phase 4: Verificação final e ajustes

### Tasks

- [ ] Task 4.1: Rodar a suíte de testes e o `tsc`; corrigir eventuais quebras (ex.: testes
      que assertam texto em PT).
- [ ] Task 4.2: Smoke test manual no browser — paleta, criar/editar/excluir chip, diálogo
      de nome, renomear I/O, Clear DB — confirmando textos em inglês e ausência de
      regressões.

### Verification

- [ ] Fluxos principais validados em inglês sem regressões.

## Final Verification

- [ ] Todos os critérios de aceite da spec atendidos
- [ ] Testes passando e `tsc` limpo
- [ ] Sem strings de UI em PT remanescentes (dev-facing mantidas conforme escopo)
- [ ] Ready for review

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
