# Implementation Plan: Reordenar Componentes da Paleta por Arrastar

**Track ID:** drag-reorder_20260621
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-21
**Status:** [ ] Not Started

## Overview

A feature é construída de baixo para cima: primeiro o modelo de ordem persistido (lógica
pura, com testes), depois a renderização ordenada da paleta, depois a alça e a mecânica de
arrasto, e por fim o polimento (toque, feedback, edge cases). Mantém o `attachPaletteDrag`
(drag-to-create) intacto, separando o gesto pela alça. Stack vanilla TS + Vite; testes com
Vitest na lógica crítica (reconciliação de ordem e cálculo de índice de destino).

## Phase 1: Modelo de ordem e persistência

Lógica pura de ordenação/reconciliação, persistida em `localStorage`, com testes.

### Tasks

- [x] Task 1.1: Criar módulo de ordem da paleta (ex.: `src/paletteOrder.ts`) com funções
      puras: reconciliar uma `paletteOrder: string[]` contra os ids de chips atuais (manter
      `'nand'`, descartar ids sumidos, anexar novos ao fim) e aplicar a ordem a uma lista de
      itens. Incluir helper de mover item de um índice para outro.
- [x] Task 1.2: Escrever testes (Vitest) cobrindo reconciliação (ids removidos/adicionados,
      `'nand'` sempre presente, ordem estável) e o move/reorder por índice.
- [x] Task 1.3: Adicionar persistência em `localStorage` (load/save de `paletteOrder`),
      tolerante a dados ausentes/corrompidos (fallback para ordem padrão).

### Verification

- [x] `npm test` passa com os novos testes da lógica de ordem.

## Phase 2: Renderização ordenada da paleta

Aplicar a ordem persistida ao montar a lista.

### Tasks

- [x] Task 2.1: `refreshPalette()` passa a renderizar NAND + chips conforme a
      `paletteOrder` reconciliada (carregada no boot), em vez da ordem crua de
      `library.list()`.
- [x] Task 2.2: Sincronizar mutações: ao criar/remover chip, atualizar e persistir a
      `paletteOrder` de forma consistente com o fluxo `onMutate`/`saveChips` existente.
      (Reconciliação + save embutidos no `refreshPalette`, chamado após cada mutação.)

### Verification

- [x] Chips e NAND aparecem na ordem persistida; criar/remover chip mantém a lista coerente.

## Phase 3: Alça e mecânica de arrasto

UI da alça e reordenação por pointer events.

### Tasks

- [x] Task 3.1: Adicionar a alça (`data-icon="drag-vertical"`) ao NAND e a cada botão de
      chip, com CSS (posição, cursor, `touch-action: none`) e alinhamento com o rótulo.
- [x] Task 3.2: Iniciar o arrasto de reordenação no `pointerdown` da alça com
      `stopPropagation` (para não acionar o drag-to-create do corpo) e pointer capture.
- [x] Task 3.3: Durante o `pointermove`, calcular o índice de destino pela posição vertical
      e refletir no DOM (reordenação ao vivo / indicador de inserção); no `pointerup`,
      gravar a nova ordem (`paletteOrder` + persistência) e re-renderizar.

### Verification

- [x] Arrastar pela alça reordena e persiste; arrastar pelo corpo ainda cria instância no
      canvas; clique/edição inalterados.

## Phase 4: Toque, feedback e edge cases

Robustez e acabamento.

### Tasks

- [x] Task 4.1: Garantir funcionamento no toque (pointer events) sem scroll/seleção
      indevida; tratar `pointercancel` e soltar fora da lista (cancela sem bagunçar).
      (`touch-action: none` na alça; pointer capture; `pointercancel` restaura a ordem.)
- [x] Task 4.2: Polir feedback visual (item arrastado em destaque) e revisar acessibilidade
      da alça — alça marcada como decorativa (`aria-hidden`), pois não há reordenação por
      teclado; o nome acessível do botão permanece o do componente.
- [x] Task 4.3: Validar lista longa com scroll (coordenadas de viewport respeitam o scroll)
      e revisar consistência visual geral.

### Verification

- [x] Reordenação fluida em mouse e toque; sem regressões no drag-to-create nem no scroll.

## Final Verification

- [ ] Todos os critérios de aceitação do spec atendidos.
- [ ] `npm run build`, typecheck, lint e testes passando.
- [ ] `npm run dev` rodando; roteiro de teste manual executado pelo usuário.
- [ ] Pronto para revisão/commit (Conventional Commits).

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
