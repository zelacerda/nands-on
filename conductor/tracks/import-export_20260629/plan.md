# Implementation Plan: Importar e Exportar Biblioteca de Chips (JSON)

**Track ID:** import-export_20260629
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-29
**Status:** [x] Complete

## Overview

Criar um módulo de serialização versionado (`transfer.ts`) com testes cobrindo o
envelope JSON, a validação de `format`/`version` e a desserialização defensiva. Em seguida,
ligar os botões **Export** e **Import** do menu (`main.ts`) a esse módulo: Export gera um
`Blob` para download; Import lê o arquivo, valida, substitui toda a biblioteca, persiste no
IndexedDB e atualiza a paleta. Por fim, polir mensagens (i18n) e tratamento de erros.

## Phase 1: Módulo de Serialização (TDD)

Lógica central de export/import isolada e testável, sem tocar no DOM.

### Tasks

- [x] Task 1.1: Criar `src/transfer.ts` com a constante `FORMAT_VERSION = '1.0'`, o tipo do
      envelope (`{ format: 'nands-on'; version: string; chips: ChipDefinition[] }`) e as
      funções `serializeLibrary(chips): string` e `parseLibrary(text): ChipDefinition[]`.
- [x] Task 1.2: Em `parseLibrary`, validar `JSON.parse` protegido, `format === 'nands-on'`,
      `version === FORMAT_VERSION` e a estrutura mínima de cada `ChipDefinition`; lançar
      erros tipados/distintos por causa (JSON inválido, formato/versão incompatível,
      estrutura inconsistente).
- [x] Task 1.3: Escrever `src/transfer.test.ts` cobrindo round-trip
      (serialize → parse), rejeição de versão diferente, format errado, JSON corrompido e
      chips malformados.

### Verification

- [x] `npm test` passa, incluindo os novos testes de `transfer`.

## Phase 2: Exportar

Botão **Export** funcional gerando download do arquivo.

### Tasks

- [x] Task 2.1: No handler de `#cmd-export` (`main.ts`), serializar a biblioteca atual via
      `serializeLibrary` e disparar download (`Blob` + `<a download>`,
      `nands-on-export.json`), fechando o menu.
- [x] Task 2.2: Garantir liberação do `URL.createObjectURL` (revoke) após o download.

### Verification

- [x] Build (`npm run build` ou `tsc --noEmit`) sem erros de tipo.

## Phase 3: Importar

Botão **Import** funcional: seleção de arquivo, validação, substituição e atualização da UI.

### Tasks

- [x] Task 3.1: Adicionar um `<input type="file" accept="application/json">` oculto no
      `index.html` (ou criado via JS) e acioná-lo a partir do handler de `#cmd-import`.
- [x] Task 3.2: Ao selecionar arquivo, ler o conteúdo, chamar `parseLibrary`, e em caso de
      sucesso **substituir toda a biblioteca**: `saveChips`, recarregar a `ChipLibrary`,
      limpar o espaço de trabalho (evita instâncias órfãs) e atualizar a paleta (sem reload).
- [x] Task 3.3: Em caso de erro, exibir mensagem clara e **preservar** o estado atual;
      resetar o `value` do input para permitir reimportar o mesmo arquivo.

### Verification

- [x] Build sem erros; menu abre, Import/Export respondem ao clique no desktop.

## Phase 4: Mensagens (i18n), Erros e Verificação Final

Polimento das mensagens e verificação manual.

### Tasks

- [x] Task 4.1: Adicionar em `strings.ts` as mensagens de sucesso/erro de import (JSON
      inválido, versão incompatível, estrutura inválida, import concluído) e usá-las nos
      handlers.
- [x] Task 4.2: Revisar acessibilidade/toque dos botões e o fechamento do menu após cada
      ação.

### Verification

- [x] Subir o dev server (`npm run dev`, em background) e fornecer roteiro de teste manual
      ao usuário: exportar, recarregar/limpar, importar de volta, e tentar importar um JSON
      de versão inválida e um corrompido.

## Final Verification

- [x] Todos os critérios de aceitação atendidos.
- [x] Testes passando (`npm test`).
- [x] Build de tipos limpo.
- [x] Pronto para revisão.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
