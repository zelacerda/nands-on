# Implementation Plan: UX da Paleta — Reordenação, Cor de I/O e Arrastar-para-Criar

**Track ID:** palette-ux_20260531
**Spec:** [spec.md](./spec.md)
**Created:** 2026-05-31
**Status:** [x] Complete

## Overview

Três frentes incrementais, da mais simples à mais envolvente: (1) ajustes visuais de baixo
risco — ordem dos botões e cor por categoria; (2) a interação de arrastar-para-criar com
Pointer Events; (3) cobertura de toque, casos de borda e verificação final. Conforme o
workflow do projeto, testes ficam concentrados na lógica não-trivial (limiar de
clique/arrasto e conversão de drop), enquanto ajustes de UI/render são validados
manualmente. Commits seguem Conventional Commits.

## Phase 1: Ajustes Visuais (ordem e cor)

Mudanças isoladas e de baixo risco que já entregam parte dos critérios de aceitação.

### Tasks

- [x] Task 1.1: Reordenar os botões `data-add` em `index.html` para **Entrada, Saída,
      NAND** (mantendo `#delete` ao final e os chips inseridos antes dele).
- [x] Task 1.2: Em `src/render.ts`, tornar a cor do corpo função da **categoria** do nó:
      `input`/`output` usam a cor de I/O (`COLOR.body`); `nand`/`chip` usam a cor de
      componente lógico (`COLOR.chipBody`). Ajustar a condição em `drawNode`.

### Verification

- [ ] Paleta exibe Entrada, Saída, NAND nessa ordem; NAND aparece no canvas com a mesma cor
      dos chips e Entrada/Saída com cor distinta. `npm run build`/typecheck sem erros.

## Phase 2: Arrastar-para-Criar

Substituir o clique-no-centro por arrastar-e-soltar, criando a instância no ponto do drop.

### Tasks

- [x] Task 2.1: Generalizar a criação para aceitar um ponto de mundo: extrair de
      `addNodeAtCenter`/`addChipInstanceAtCenter` funções que recebem a posição de drop
      (ex.: `addNodeAt(type, worldPoint)` / `addChipInstanceAt(def, worldPoint)`), centrando
      a instância pelo `chipSize`/`NODE_SIZE`.
- [x] Task 2.2: Implementar o gesto de arrasto a partir dos botões da paleta com Pointer
      Events: `pointerdown` no botão + `setPointerCapture`, rastrear `pointermove`, e em
      `pointerup` decidir entre **clique** (deslocamento < limiar → não faz nada) e
      **arrasto** (≥ limiar → criar instância).
- [x] Task 2.3: No `pointerup` de arrasto, converter a posição para coordenadas locais do
      canvas e então para mundo (`camera.screenToWorld`); criar a instância apenas se o drop
      cair **dentro** da área do editor (caso contrário, no-op).
- [x] Task 2.4: Aplicar o mesmo gesto aos botões de chips criados em `refreshPalette`
      (substituindo o `click → addChipInstanceAtCenter`), unificando a anexação do handler
      de arrasto para primitivas e chips.
- [x] Task 2.5: (lógica) Testar o limiar clique/arrasto e a conversão drop→mundo onde a
      lógica for extraível (ex.: função pura de classificação do gesto e/ou de cálculo do
      ponto centrado), seguindo a política de TDD flexível.

### Verification

- [ ] Arrastar qualquer botão (primitiva ou chip) cria a instância no ponto solto; clique
      simples não cria nada; soltar fora do editor não cria nada. Testes da lógica passam.

## Phase 3: Toque, Bordas e Polimento

Garantir paridade mobile e robustez da interação.

### Tasks

- [x] Task 3.1: Validar o fluxo em **toque** (pointerType `touch`): captura, limiar
      adequado e ausência de scroll/seleção indesejada ao arrastar do botão (ajustar
      `touch-action`/`user-select` em `src/style.css` se necessário).
- [x] Task 3.2: Tratar cancelamento (`pointercancel`) e múltiplos ponteiros sem deixar
      estado de arrasto preso; garantir que o gesto da paleta não conflita com pan/pinça do
      canvas.
- [x] Task 3.3: Limpeza final — remover código morto do antigo clique-no-centro e revisar o
      próprio diff (auto-review).

### Verification

- [ ] Interação funciona com mouse e toque; sem estados presos após cancelamento; sem
      regressões em pan/zoom/pinça.

## Final Verification

- [x] Todos os critérios de aceitação do spec atendidos
- [x] Testes da lógica passando (`npm test`); build/typecheck limpos
- [ ] Verificação manual no encerramento do track (desktop + toque)
- [x] Pronto para revisão

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
