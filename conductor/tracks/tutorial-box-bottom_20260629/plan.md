# Implementation Plan: Mover Box do Tutorial para a Parte Inferior

**Track ID:** tutorial-box-bottom_20260629
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-29
**Status:** [ ] Not Started

## Overview

Ajuste isolado de CSS em `src/style.css`: reancorar o `#tutorial-callout` do topo para o
rodapé, posicionando-o logo acima da barra de edição (`#action-bar`) com uma folga segura.
Sem mudanças de JS ou de conteúdo do tutorial. Verificação visual em viewport vertical de
celular e em desktop.

## Phase 1: Reposicionamento

Mover o callout para a parte inferior, acima do `#action-bar`.

### Tasks

- [ ] Task 1.1: Em `#tutorial-callout` (`src/style.css`), substituir `top: 16px` por uma
      ancoragem ao rodapé com folga segura acima do `#action-bar` (a barra ocupa ~60px:
      `bottom: 16px` + botões de 44px) — ex.: `bottom: 76px`. Manter `left: 50%`,
      `transform: translateX(-50%)`, largura e `z-index` atuais.
- [ ] Task 1.2: Conferir que o seletor de `[hidden]` e o posicionamento do botão "×"
      (`#tutorial-exit`, absoluto relativo ao callout) seguem corretos após a mudança.

### Verification

- [ ] `npm run build` (tsc + vite) sem erros.

## Phase 2: Verificação Visual

Validar o comportamento em telas estreitas/verticais e em desktop.

### Tasks

- [ ] Task 2.1: Subir o dev server e fornecer roteiro de teste manual: iniciar o tutorial,
      confirmar que o box aparece no rodapé acima do `#action-bar` (com folga), que os
      primeiros chips da paleta ficam livres em viewport vertical de celular, e que o "×" e
      o texto seguem legíveis. Conferir desktop sem regressão.

### Verification

- [ ] Critérios de aceitação confirmados visualmente pelo usuário.

## Final Verification

- [ ] Todos os critérios de aceitação atendidos.
- [ ] Build limpo.
- [ ] Pronto para revisão.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
