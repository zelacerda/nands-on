# Implementation Plan: Renomear Entradas/Saídas e Rótulos de Pinos

**Track ID:** rename-io_20260602
**Spec:** [spec.md](./spec.md)
**Created:** 2026-06-02
**Status:** [ ] Not Started

## Overview

Adicionar nome aos nós input/output e propagá-lo aos pinos do `ChipDefinition`, renderizar
rótulos pequenos junto aos pinos (incl. A/B/Q padrão no NAND), e construir a UI de edição
por duplo clique via overlay HTML `<input>` com botão de overline. TDD aplicado à lógica de
modelo/captura; UI verificada manualmente pelo usuário no servidor já em execução.

## Phase 1: Modelo e Captura de Nomes

Estabelecer o campo de nome no modelo e garantir que ele flua até o chip criado.

### Tasks

- [x] Task 1.1: Adicionar campo de rótulo de pino ao modelo (`label`/`name` em `Pin` e/ou
  uso do `name` do nó input/output) em `model.ts`.
- [x] Task 1.2: Definir rótulos padrão do NAND (A superior, B inferior, Q saída) de forma
  centralizada (resolução na renderização, sem poluir o modelo).
- [x] Task 1.3: Atualizar `captureDefinition` em `chip.ts` para transferir os nomes dos nós
  input/output para os pinos do `ChipDefinition` (mantendo a ordenação por posição).
- [x] Task 1.4: Escrever testes para `captureDefinition` cobrindo nomes preenchidos,
  ausentes (fallback) e ordenação.

### Verification

- [x] Testes da captura passam (`npm test`); um chip "Fazer" gera pinos com os nomes
  esperados.

## Phase 2: Renderização dos Rótulos

Desenhar os nomes junto aos pinos no canvas.

### Tasks

- [x] Task 2.1: Em `render.ts`, desenhar rótulo em fonte pequena junto a cada pino de chips
  compostos (dentro do retângulo), sem cobrir o nome central.
- [x] Task 2.2: Renderizar A/B/Q no NAND nas posições corretas dos pinos.
- [x] Task 2.3: Renderizar corretamente o overline (U+0305) no canvas (medição/posicionamento
  do glifo combinante).
- [x] Task 2.4: Ajustar layout/largura mínima do chip se os rótulos exigirem mais espaço.

### Verification

- [~] Validação manual do usuário no servidor: NAND mostra A/B/Q; um chip composto mostra os
  nomes dos pinos legíveis e bem posicionados.

## Phase 3: UI de Edição por Duplo Clique

Editar o nome dos nós input/output via overlay.

### Tasks

- [x] Task 3.1: Detectar duplo clique sobre nó `input`/`output` em `main.ts` (via `dblclick`
  ou detecção de toque duplo), usando `hitNode`.
- [x] Task 3.2: Exibir overlay HTML `<input>` posicionado sobre o nó (screen coords da
  câmera), reaproveitando o padrão do diálogo "Fazer".
- [x] Task 3.3: Confirmar (Enter/blur) grava o nome no nó; Esc cancela. Funciona com mouse e
  toque.
- [x] Task 3.4: Adicionar botão auxiliar de barra superior que aplica/remove U+0305 sobre o
  texto do campo.

### Verification

- [~] Validação manual do usuário no servidor: duplo clique renomeia o nó; o nome aparece no
  chip ao "Fazer"; botão de overline produz `Q̄`.

## Phase 4: Polish e Verificação Final

### Tasks

- [x] Task 4.1: Tratar casos de borda (nome vazio → fallback; nomes longos;
  truncamento/elipse).
- [x] Task 4.2: Garantir interação com mobile (teclado virtual, foco, não conflitar com
  pan/zoom).

### Final Verification

- [x] Todos os critérios de aceitação atendidos (implementação).
- [x] Testes passando (71 testes; typecheck, lint e build limpos).
- [~] Verificação manual feita pelo usuário no servidor que já está de pé (mouse + toque).
- [~] Pronto para revisão.

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
